{
  "buildCommand": "dashboard && npm install && npm run build",
  "outputDirectory": "dashboard/dist",
  "installCommand": "dashboard && npm install",
  "framework": null,
  "devCommand": "dashboard && npm run dev"
}
